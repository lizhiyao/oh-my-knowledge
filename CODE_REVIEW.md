# OMK 自主代码审查 Playbook

这份 playbook 把代码审查定义为交付过程的一部分，而不是等待用户触发的额外动作。它面向所有参与 OMK 开发的 Agent 与人类贡献者，不依赖特定模型、工具或多 Agent 能力。

## 核心原则

- 审查目标是发现可证明会影响用户、测量可信度、系统边界或长期演进的问题，不是生成尽可能长的问题清单。
- 测试通过只能证明已经覆盖的断言成立，不能单独证明需求完整、架构合理、公开契约稳定或真实安装可用。
- 审查深度与风险匹配。低风险改动快速复核；高风险改动覆盖完整矩阵并验证真实边界。
- 实现和审查采用不同视角。实现完成后先停止修改，从完整 diff、调用方和公开结果重新理解改动，再记录 findings。
- 不确定但无法证明的问题先调查；仍无证据时写入风险说明，不伪装成确定 finding。

## 风险分级

| 等级 | 典型改动 | 最低审查深度 |
|---|---|---|
| 高 | Evaluation Core、评分／统计／prompt、公开 Schema／API、持久化与迁移、执行器／子进程、安全边界、并发／锁／清理、npm 发版 | 完整审查矩阵、分层测试、`yarn ci`、对应真实路径或 clean-room 验收 |
| 中 | CLI 行为、错误处理、文件系统／网络边界、非平凡领域重构、报告 UI、跨模块契约 | 所有相关维度、定向单元／集成测试、`yarn ci`；按边界补真实验收 |
| 低 | 局部文档、测试描述、无行为变化的类型或机械重命名 | 完整 diff 快检、链接／格式／受影响检查；交付前仍遵守仓库门禁 |

任一高风险因素出现，整体按高风险处理。不能因为 diff 行数少而降低公开契约或测量学改动的等级。

## 工作流

### 1. 开工前建立风险地图

先写清四件事：

1. 用户要解决的问题与可观察的成功结果。
2. 明确非目标，尤其是不应顺手增加的命令、兼容层或抽象。
3. 受影响的领域、公开入口、持久化数据和测量学不变量。
4. 风险等级，以及需要证明哪些真实边界。

需求不清时先调查；只有缺少会实质改变结果的用户决策时才停下来询问。

### 2. 实现中保持可审查

- PR 应围绕一个完整目标，避免把无关清理混入，也不要为追求极小而拆断同一个可验证闭环。
- 先跑最窄的有效验证获得快速反馈；相关修复成批完成后再跑完整门禁并 push。
- 新增失败分支时，同时设计可操作诊断和回归测试。
- 测试必须 hermetic：使用显式临时目录和环境隔离，结束后不得修改仓库受管文件或用户状态。

### 3. 实现后做 fresh-pass CR

先只读检查 `git diff`、新增／删除文件、公开 exports、Schema／snapshot 变化和调用方，再按下面矩阵审查。此阶段不要只复述实现意图；从失败机制出发验证代码实际行为。

能使用独立 reviewer 时，可以让其只读审查明确范围；不能使用时，当前 Agent 清空实现假设后完成同一套 fresh-pass。是否多 Agent 不影响验收标准。

CR 工具产物必须与源码工作树隔离。每轮使用 `mktemp -d` 创建仓库外运行目录，把材料快照、验证证据、图谱、上下文、编排结果和报告统一放入其中；不得在仓库内生成 `artifacts/cr/` 或 `knowledge/cr/`，也不得用 `.gitignore` 掩盖工具写入。PR、CI 和经用户明确授权的外部报告承担持久留痕；只有经过正常评审、确属项目资产的文档才进入仓库。

### 4. 修复与最终复审

- 先按优先级修复所有 P0～P2，再成批运行受影响测试。
- 修复若改变架构、公开行为或风险地图，重新执行完整 CR。
- 其它修复只复查受影响维度和最终 diff，避免无意义地从头循环。
- 完整本地门禁应在最终改动集上运行，而不是引用修复前结果。
- CR 结束后复查 `git status --short`，确认工具没有留下材料、缓存、报告或其它非目标文件。

## 多维审查矩阵

| 维度 | 必查问题 | 常见证据 |
|---|---|---|
| 需求与范围 | 是否解决原问题？成功标准是否可观察？是否引入未授权能力或过度抽象？ | issue、非目标、最终 diff、用户路径 |
| 架构与依赖 | 代码是否归属正确领域？依赖是否单向？Core 是否保持宿主无关？公开入口是否最小？ | 目录结构、import graph、exports、边界守卫 |
| 正确性与失败路径 | happy path、空输入、非法输入、部分失败、重试和异常传播是否一致？ | 分支矩阵、调用链、负向测试、错误 envelope |
| 测量学 | 是否改变 construct、样本选择、五层评分、统计公式、prompt、缺失证据或可比性？ | 冻结 hash、Schema diff、报告字段、统计测试 |
| API／Schema／存储 | 公开 identity 与物理路径是否混淆？读写协议、版本、迁移／不兼容决策是否明确？ | package exports、JSON Schema `$id`、落盘 fixture、迁移说明 |
| 安全与隐私 | 外部输入、路径、URL、env、凭证、子进程和报告输出是否跨越信任边界？失败是否 fail-closed？ | 输入校验、escaping、redaction、权限失败测试 |
| 状态／并发／资源 | 是否幂等、原子、可恢复？锁、缓存、临时文件、session 和进程在成功／失败／中断后是否正确释放？ | 并发测试、cleanup 路径、原子 rename、资源计数 |
| 用户体验与 i18n | 文案是否准确、可操作、不泄密？帮助、exit code、中英文和报告呈现是否一致？ | Command 集成测试、真实 stderr／stdout、截图、双语文档 |
| 测试可信度 | 测试是否命中真实分支而非 mock 假象？是否覆盖领域层与接线层？是否污染仓库或依赖执行顺序？ | 定向测试、hermetic guard、重复／并行运行 |
| 打包与交付 | source 可用是否等于 dist／tarball 可用？subpath、类型声明、生成文件、Node 矩阵和 release notes 是否正确？ | clean build、npm pack、隔离安装、远端门禁 |

## Finding 规范

严重度：

- **P0**：会造成错误测量结论、严重安全问题、不可恢复数据损坏或系统整体不可用，必须立即阻塞。
- **P1**：常见路径行为错误、公开契约破坏、关键门禁失效或高概率生产故障，必须在交付前修复。
- **P2**：有现实触发条件的边界错误、诊断缺陷、资源泄漏或会持续放大的设计债务，默认在当前 PR 修复。
- **P3**：不阻塞目标的局部改进；修复或登记明确 follow-up，不得用 P3 隐藏未解决的正确性问题。

每个 finding 使用下面的最小结构：

```text
[P1] 简短标题 — path/to/file.ts:line
证据：哪段代码／契约与什么输入组合触发。
机制：为何会失败，而不只是描述现象。
影响：用户行为、数据、API 或测量可信度如何受损。
修复：最小且架构一致的修复方向。
验证：能阻止回归的测试或真实验收。
```

Finding 必须定位到可操作代码或契约。风格偏好、没有触发机制的担忧和与本 PR 无关的旧问题不作为阻塞 finding；必要时单独建 issue。

## 验证阶梯

按风险从低到高组合，不要求所有改动机械执行全部层级：

1. 静态检查：完整 diff、`git diff --check`、类型、lint、生成文件与 snapshot diff。
2. 领域测试：直接覆盖修改逻辑的成功、失败和边界分支。
3. 接线测试：覆盖 CLI／API 参数到领域行为以及输出 envelope。
4. 完整门禁：最终改动集运行 `yarn ci`。
5. 真实路径：用生产入口验证用户实际看到的 stdout、stderr、文件或 UI。
6. Clean-room：公开包、exports、Schema 分发、安装／初始化、模块加载环境或发版改动使用 tarball／隔离目录验证。

真实验收必须保持低成本、可重复和隔离。不要用真实用户仓库、全局状态或远端 CI 代替本地反馈环。

## 完成与停止标准

满足以下条件才可声明完成：

- 目标和非目标没有漂移。
- P0～P2 全部解决；P3 已解决或有明确 follow-up。
- 最终 diff 已复审，没有新 P0～P2。
- 定向验证、`yarn ci` 和风险要求的真实验收均基于最终代码通过。
- 测量学、兼容／迁移和剩余风险已在 PR 描述明确说明。

完成后不要因为“也许还能再看一遍”无限重复 CR。只有以下事件触发新的完整审查：

- 架构、公开 API、Schema、存储或用户行为发生实质变化。
- CI 或真实验收暴露新失效机制。
- rebase／冲突解决改变关键代码。
- 外部 reviewer 提出新的高风险方向。

## 审查输出

有 findings 时，按严重度排序，先给问题再给总评。无 findings 时明确写 `No findings`，随后简述已检查的高风险面、验证证据和残余风险。不要用测试清单掩盖尚未分析的架构或测量问题。

自主 CR 的结论写入 PR 描述；用户显式要求对已存在 PR 做 `cr`／`review` 时，按 `AGENTS.md` 要求把审查评论直接发布到对应 PR。

## 指令发现 Smoke

修改 `AGENTS.md`、嵌套领域规则或 Agent 接入配置时，除静态治理测试外做一次低成本人工验证：

1. 从仓库根启动本机可用的 Agent，请它列出当前仓库规则来源，确认能发现根 `AGENTS.md` 与 `CODE_REVIEW.md`。
2. 以有嵌套规则的目录为工作目录启动 Agent，确认根规则与距离该目录最近的领域 `AGENTS.md` 同时生效。
3. Codex 走原生 agents.md 发现，Claude Code 走 `CLAUDE.md` import。其它工具只在仓库实际使用时增加接入配置，并按其官方发现机制验收。

CI 只验证配置存在、语法和关键规则锚点，不在测试中启动第三方 Agent，也不把静态检查误当成实际指令加载证明。
